# merml-mcp

Domain MCP server — RC Merchandising reads (prices, promotions, markdowns).

Tools: `get_current_price`, `list_active_promotions`, `list_recent_markdowns`.

Required caller scope: `practice:rc`.
