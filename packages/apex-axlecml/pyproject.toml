[project]
name = "apex-axlecml"
version = "0.1.0"
description = "APEX AXLECML — Automotive / Industrial Manufacturing canonical entities over ISA-95 + SAE J1939 + ISO 14224."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary — Deloitte Internal" }
dependencies = [
    "apex-core",
    "apex-schemas-common",
    "apex-standards-isa95",
    "apex-standards-j1939",
    "apex-standards-iso14224",
    "pydantic>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apex_axlecml"]

[tool.uv.sources]
apex-core = { workspace = true }
apex-schemas-common = { workspace = true }
apex-standards-isa95 = { workspace = true }
apex-standards-j1939 = { workspace = true }
apex-standards-iso14224 = { workspace = true }
