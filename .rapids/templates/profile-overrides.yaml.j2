# Profile Overrides — {{ pack_id }}@{{ pack_version }}
# Generated by RAPIDS

overrides:
{% for override in overrides %}
  - scope: {{ override.scope }}
    apply: {{ override.profile }}
    rationale: "{{ override.rationale }}"
{% endfor %}
