# telemetry-mcp

Utility MCP server — trace + log + latency-percentile queries.

Tools: `emit_trace`, `log_event`, `query_latency_percentile`.

Sprint 7 ships an in-memory sink. Sprint 13 wires App Insights / OpenTelemetry.
