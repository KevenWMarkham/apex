# =====================================================================
# APEX-M · Local Setup · requirements.txt
# =====================================================================
# Derived from apex-m/pyproject.toml on 2026-05-29.
#
# NATIVE INSTALL (recommended): uv sync --extra runtime --extra dev
#                               from the APEX repo root. This file
#                               is the pip-equivalent fallback.
#
# Python: requires-python = ">=3.12" (HARD requirement)
# =====================================================================

# ----- Workspace-local (install in editable mode FIRST) ---------------
# pip install -e packages/apex-core
# (NOT pulled from PyPI — lives in the APEX monorepo workspace)

# ----- Core runtime (always installed) -------------------------------
azure-identity>=1.19
azure-keyvault-secrets>=4.9
azure-cosmos>=4.7
azure-monitor-opentelemetry>=1.6
azure-mgmt-cognitiveservices>=14.0
pydantic>=2.9
httpx>=0.27
pyyaml>=6.0

# ----- Runtime extras (real Microsoft tenant) ------------------------
# Lazy-imported by apex_m.* impls.
# Skip these if running APEX_FORCE_MOCK=true exclusively.
azure-ai-projects>=1.0.0b3
msal>=1.31

# ----- Microsoft Agent Framework (canonical orchestrator per ADR-006) -
# https://learn.microsoft.com/agent-framework/
agent-framework>=1.0.0
agent-framework-azure-ai>=1.0.0

# ----- Dev tooling ---------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.7
mypy>=1.13

# ----- Integration test lane (real Lab tenant via WIF) ---------------
# Required only when running `pytest -m integration` against a Lab
# subscription. Safe to install everywhere; the marker gates execution.
pytest-integration>=0.2
