# scml-mcp

Domain MCP server — RC Supply Chain canonical reads. Wraps `fabric-mcp` with SCML-entity-aware scoping.

Tools: `get_sku_by_key`, `list_skus_by_supplier`, `list_shipments_by_destination`.

Required caller scope: `practice:rc`.
