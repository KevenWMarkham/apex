# Workflow for archetype apex-core-component
# 6-phase orchestration with APEX governance gates
# TODO: detailed phase definitions (mirrors workflows/pack-new.yaml shape)

workflow:
  id: apex-core-component
  version: 0.1.0  # SKELETON · expand in PR
  archetype: apex-core-component
  status: skeleton
